# Critical Rendering Path (CRP)

## Зачем это нужно

Critical Rendering Path — это цепочка шагов, которые браузер **обязан выполнить**, прежде чем пользователь увидит первый контент.

Понимание CRP позволяет:
- ускорять загрузку страниц
- улучшать FCP / LCP
- осознанно работать с CSS и JS
- объяснять, *почему сайт долго белый*

На собеседованиях CRP — частый вопрос для Middle+.

---

## Краткое определение

**Critical Rendering Path** — это последовательность:
HTML → DOM → CSSOM → Render Tree → Layout → Paint

Ключевая идея:
> Пока CRP не завершён, пользователь ничего не видит.

---

## Блокирующие ресурсы

### HTML
- парсится сверху вниз
- `<script>` без атрибутов **блокирует парсинг**

---

### CSS — всегда блокирует рендер

```html
<link rel="stylesheet" href="style.css">
```

Почему:
- без CSS нельзя построить Render Tree
- браузер ждёт CSSOM

---

### JavaScript — условно блокирующий

```html
<script src="app.js"></script>
```

Что происходит:
- HTML-парсер останавливается
- JS выполняется
- только потом парсинг продолжается

---

## async и defer

### defer

```html
<script src="app.js" defer></script>
```

- HTML парсится до конца
- JS выполняется **после парсинга**
- порядок скриптов сохраняется

👉 Лучший выбор для большинства случаев.

---

### async

```html
<script src="app.js" async></script>
```

- HTML парсится параллельно
- JS выполняется **сразу после загрузки**
- порядок не гарантирован

👉 Подходит для аналитики.

---

## Оптимизация CRP

### 1. Минимизация CSS

- удаление неиспользуемых стилей
- split на critical / non-critical CSS

---

### 2. Critical CSS

- CSS для above-the-fold
- инлайн в `<style>`

---

### 3. Defer JS

- перенос JS из head
- использование `defer`

---

### 4. Preload / Preconnect

```html
<link rel="preload" as="font" href="font.woff2">
<link rel="preconnect" href="https://api.example.com">
```

---

## Типовые ловушки

### Ловушка 1 — большой CSS в head

Даже если он нужен «потом», он блокирует первый рендер.

---

### Ловушка 2 — JS в head без defer

Белый экран + плохой UX.

---

## Связь с метриками

- **FCP** — когда CRP завершён частично
- **LCP** — когда загружен главный контент
- **CLS** — зависит от корректного layout

---

## Как отвечать на собеседовании

1. CRP — путь до первого рендера
2. CSS блокирует рендер всегда
3. JS блокирует без async/defer
4. defer — лучший дефолт
5. Critical CSS ускоряет FCP

---

## Вопросы для самопроверки

1. Почему CSS блокирует рендер?
2. Чем async отличается от defer?
3. Что такое critical CSS?
4. Как preload помогает CRP?
5. Где чаще всего ошибаются?

---

## Связанные темы

- Rendering pipeline
- Performance metrics
- HTML loading
- SEO
