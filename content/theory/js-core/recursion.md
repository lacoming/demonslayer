# Рекурсия в JavaScript и её опасности (Stack Overflow)

## Зачем это нужно

Рекурсия — это техника, где функция вызывает саму себя. Она часто встречается:
- при обходе деревьев (DOM, AST)
- в алгоритмах (DFS, backtracking)
- в парсинге и обработке вложенных структур

На собеседованиях рекурсия проверяет:
- понимание **call stack**
- умение видеть базовый случай
- осознание рисков переполнения стека

---

## Краткое определение

**Рекурсия** — это функция, которая вызывает саму себя до тех пор, пока не будет достигнут **базовый случай**.

Ключевая идея:
> Без базового случая рекурсия бесконечна и ломает стек вызовов.

---

## Как работает рекурсия (модель в голове)

Каждый вызов функции:
- кладётся в **call stack**
- занимает память
- ждёт завершения вложенного вызова

```js
function count(n) {
  if (n === 0) return;
  count(n - 1);
}
```

Стек:
```
count(3)
count(2)
count(1)
count(0)
```

После достижения базового случая стек начинает “сворачиваться”.

---

## Базовый случай — главное правило

```js
function factorial(n) {
  if (n === 1) return 1; // базовый случай
  return n * factorial(n - 1);
}
```

❗ Без базового случая будет:
```
RangeError: Maximum call stack size exceeded
```

---

## Пример: рекурсия vs цикл

### Рекурсия

```js
function sum(arr, i = 0) {
  if (i === arr.length) return 0;
  return arr[i] + sum(arr, i + 1);
}
```

### Цикл (без риска)

```js
let total = 0;
for (const x of arr) {
  total += x;
}
```

Цикл безопаснее для больших данных.

---

## Stack Overflow

**Stack Overflow** — переполнение стека вызовов.

Причины:
- слишком глубокая рекурсия
- отсутствие базового случая
- рекурсия на больших структурах

```js
function boom() {
  boom();
}
boom(); // 💥
```

---

## Tail Recursion (важно понимать, но не рассчитывать)

**Хвостовая рекурсия** — когда рекурсивный вызов последний в функции.

```js
function sum(n, acc = 0) {
  if (n === 0) return acc;
  return sum(n - 1, acc + n);
}
```

❗ В JavaScript **нет гарантированной оптимизации хвостовой рекурсии**.
Не стоит на неё полагаться.

---

## Когда использовать рекурсию

Используй, когда:
- структура данных рекурсивна (деревья)
- глубина контролируема
- код становится понятнее

Избегай, когда:
- большие массивы
- неизвестная глубина
- можно обойтись циклом

---

## Как отвечать на собеседовании

1. Рекурсия — функция вызывает саму себя
2. Обязателен базовый случай
3. Каждый вызов занимает место в call stack
4. Возможен stack overflow
5. В JS лучше избегать глубокой рекурсии

---

## Вопросы для самопроверки

1. Что такое базовый случай?
2. Почему возникает stack overflow?
3. Чем рекурсия опаснее цикла?
4. Что такое хвостовая рекурсия?
5. Почему нельзя на неё рассчитывать в JS?

---

## Связанные темы

- Call stack
- Event Loop
- Алгоритмы и структуры данных
- Производительность
